<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('report_builder_report_schedules', function (Blueprint $table): void {
            $table->id();
            $table->foreignId('saved_report_id')->constrained('report_builder_saved_reports')->cascadeOnDelete();
            $table->boolean('enabled')->default(true)->index();
            $table->string('frequency_type', 20)->index();
            $table->string('cron_expression')->nullable();
            $table->string('timezone', 64)->default('UTC');
            $table->string('format', 20)->default('csv');
            $table->json('recipients')->nullable();
            $table->timestamp('last_run_at')->nullable()->index();
            $table->timestamp('next_run_at')->nullable()->index();
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('report_builder_report_schedules');
    }
};
