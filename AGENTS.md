# Report Builder Package Guidelines

- This is a Laravel package built from Spatie's package skeleton.
- Use strict typing.
- Add tests for every new feature.
- Keep implementation modular and small.
- Do not build UI, exporters, scheduling, or database persistence unless explicitly requested.
- Prefer registered source classes over arbitrary model class strings.
- Keep public APIs clean and intentionally designed.